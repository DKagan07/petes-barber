import type { NextConfig } from "next";

const nextConfig: NextConfig = {
    /* config options here */
    // Example of an external image on the app
    // images: {
    //     remotePatterns: [
    //         {
    //             protocol: "https",
    //             hostname: "d2zdpiztbgorvt.cloudfront.net",
    //             port: "",
    //         },
    //     ],
    // }
}

export default nextConfig;
